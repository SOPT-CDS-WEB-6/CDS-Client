import { BrowserRouter, Routes } from 'react-router-dom';

const Router = () => {
  return (
    <BrowserRouter>
      <Routes>
        {/* home */}
        {/* movieInfo */}
        {/* mymovie */}
        {/* inquiry */}
      </Routes>
    </BrowserRouter>
  );
};

export default Router;
